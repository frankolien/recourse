# Backend secrets

Place the Sign in with Apple private key here as:

```text
AuthKey_XY9C96WBZL.p8
```

The `.p8` file is ignored by Git. It must remain on the backend only and must never
be copied into the iOS app, committed, or shared in chat.
