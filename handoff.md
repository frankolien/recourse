# handoff.md

Rolling operational file. Read this first every session: blockers, then next actions, then standing rules. The full spec lives in docs/recourse/. This file is distinct from the handoff doc; it is the live worklist.

## Live deployment (Arc testnet, chainId 5042002)

Fixed-contract deploy, verified on-chain 2026-07-20 (includes the settlement-underflow
fix). Addresses in deployments/arc-testnet.json; explorer https://testnet.arcscan.app/address/<addr>.

| Contract | Address |
|---|---|
| RecourseEscrow | 0x61Fd99789B28582882a3369E2024AeaE5b5D2DC0 |
| PolicyRegistry | 0x94f8551fbE43aB919D87c3951394b148c914430E |
| SettlementVault | 0x5d8a3000866493f5D0B5B07a4Ad63ADE3B02054D |
| MockUSYCAdapter | 0x2336AaBE139b7F426aF63f713b9f93CD3FFC6204 |
| USDC (Circle) | 0x3600000000000000000000000000000000000000 |

(Superseded first deploy, kept for reference: escrow 0x18BfF4cF4c0843EF17c0f12e7E5C940683e930a1.)

Wiring verified: escrow points at usdc/registry/adapter/vault; resolveDelay 60, yieldFeeBps 1000;
attestor and treasury both set to the deployer 0xD6c574461d96Ee708f58Fe553049aD4f48BB983A;
adapter holds a 10 USDC yield buffer. To redeploy, re-run the deploy runbook below; a redeploy
means re-running codegen and re-committing arc-testnet.json.

## Blockers

1. USYC testnet access not yet requested. Apply via the Circle faucet/portal. Until approved, MockUSYCAdapter is the wired adapter; the swap to a USYCTellerAdapter (Teller at 0x9fdF14c5B14173D74C08Af27AebFf39240dC105A) is a redeploy. Not blocking anything else.
2. The demo attestor currently equals the deployer key. When the Rust attestor bot lands, either give it this key or rotate via escrow.setAttestor to the bot's address.

## Deploy + seed runbook

Redeploy (run from contracts/ for the forge step, repo root for the rest):
```
export ARC_RPC_URL=https://rpc.testnet.arc.network
export RECOURSE_USDC=0x3600000000000000000000000000000000000000
(cd contracts && forge script script/Deploy.s.sol:Deploy --rpc-url $ARC_RPC_URL --private-key $DEPLOYER_PK --broadcast)
cast send $RECOURSE_USDC "transfer(address,uint256)" $(node -e "console.log(require('./deployments/arc-testnet.json').yieldAdapter)") 10000000 --rpc-url $ARC_RPC_URL --private-key $DEPLOYER_PK
node ops/codegen.mjs
```
Then seed the demo state. The seeder is viem, not forge: Arc's USDC is a native-token
precompile that forge's local EVM cannot execute (StackUnderflow during simulation), so
seeding goes through direct RPC broadcast. Needs the deployer funded with ~10 USDC; it
funds the buyer and merchant in-script (small amounts, 0.25 USDC per payment). DEPLOYER_PK
is the attestor. On Arc the buyer and merchant default to fresh random keys because the
well-known anvil keys are on Arc USDC's blocklist; set SEED_BUYER_PK / SEED_MERCHANT_PK to
override.
```
node engine/scripts/seed.mjs        # defaults to deployments/arc-testnet.json
```
The seed writes deployments/seed-arc-testnet.json with the notable paymentIds (refund,
deny, advanced) for the verify-page demo. Dry-run verified end to end on local anvil
(deploy via forge, seed via viem). The forge seeder was removed for the reason above.

## Next actions (architecture section 11 order, dependency-true)

0. mobile/: M4.1 native foundation, buyer business logic, and the complete live Arc contract gateway are built for iPhone and iOS 17+. `mobile/Recourse.xcodeproj` is generated deterministically by `mobile/scripts/generate_project.rb`, targets Swift 6 strict concurrency, preserves the checked-in Swift package lock, includes a shared test scheme, and runs `ops/codegen.mjs` before compilation. Codegen emits the ignored `mobile/Recourse/Generated/Deployment.swift` from `deployments/arc-testnet.json` and the RPC URL from `deployments/arc-config.json`; addresses still come only from the deployment file (R3). web3swift 3.3.2 is pinned exactly. `ArcContractGateway` composes reviewed ABI reads and writes, while `HTTPArcRPCTransport` handles eth_call, nonce, gas estimation, gas price, raw transaction submission, and receipt polling. Live reads cover balanceOf, allowance, getPolicy, policyHash, getPayment, previewVerdict, and resolveDelay. Writes cover approve, pay, fileDispute, and resolve. `TestnetLocalSigner` creates an encrypted Ethereum V3 keystore, stores it and a random password as separate `WhenUnlockedThisDeviceOnly` Keychain records, and signs legacy EIP-155 transactions locally. `DeviceOwnerTransactionAuthorizer` requires Face ID, Touch ID, or device passcode before every signing operation and runs before credentials are loaded; a denial test proves credentials remain untouched. Raw Arc response fixtures verify decoding, exact calldata fixtures verify every write, and signer tests verify persistence, deterministic signing, reset, and authorization. `ArcLiveReadTests` remains opt-in and previously passed against Arc testnet. The complete offline suite reports 29 passed, 2 intentionally skipped integration tests, and 0 failed, with no live transaction broadcast. `mobile/scripts/verify_local_writes.sh` is the R13 proof: it starts disposable anvil, deploys and seeds from canonical local deployment JSON, then executes the real Swift approve, pay, fileDispute, and resolve path. Its result reports 1 passed, 0 skipped, and 0 failed; receipts and chain state reconcile at every step, and the final 100% refund restores the buyer's USDC balance. The UI remains a thin shell. Next: bind checkout, payment detail, dispute capture, and verdict workflows to buyer screens, then exercise Face ID on a physical iPhone. Android remains deferred and may use Flutter later against the same QR, API, ABI, and deployment contracts.

1. web/: the root route / is now a marketing landing page (components/landing-page.tsx) matching the supplied reference: sticky nav, a serif hero with product mockups, a Powered by Circle strip, and a feature grid. Launch App routes through /signin, while the live public demo stays at /verify/5. The fintech-style demo onboarding is implemented at /signin and /onboarding: simulated Google, passkey, email, and existing-wallet entry; Merchant and Liquidity Provider workspace selection (buyers use the mobile app, per R5); profile setup; role-specific workspace setup; and a completion screen before /dashboard. The prototype stores the demo profile in localStorage under recourse.demo-profile and labels authentication as simulated. The dashboard greeting and topbar profile now read that state instead of hardcoding Frank. This is a UX prototype, not real authentication or an embedded wallet integration. Replace the local store when a real auth and wallet provider is selected. The landing headlines use a --serif token (Georgia) scoped under .landing; the rest of the app stays on Geist. The merchant app is now a full multi-page site. A shared MerchantShell (in components/) renders the sidebar and a persistent topbar with pathname-driven active state, mounted via the app/(merchant) route group layout. Real routes exist for /dashboard, /payments, /protection, /disputes, /receipts, /vault, /policies, /settings, /support; /policies reads policy #1 live from the registry; /vault and /settings show the deployed contract addresses with ArcScan links. The public verifier remains at /verify/5 and /verify/6 with live Arc reads, browser recomputation, exact hash comparison, and an evidence sandbox. The visual policy builder is built at /policies/new: it authors rules, live-compiles to the policy hash via the engine compiler, shows the compiled structs, tests a claim through compute, and publishes on-chain. Wallet connect is wired with wagmi v2 (injected connector only, per F1), scoped to the app/(merchant) route group via components/providers.tsx so the landing stays static; the connected address is the policy merchant, so the builder's previewed hash equals what registerPolicy pins. The Publish to Arc panel calls registerPolicy through useWriteContract (ABI-correct, but the live tx needs a funded Arc wallet to exercise), and next.config.ts ignores wagmi's unused connector optional deps (@x402/*, react-native async storage, pino-pretty). The demo onboarding remains a simulated auth prototype; wallet connect (real) sits alongside it for on-chain actions. The four merchant lists (payments, disputes, receipts, protection) now read live from the indexer API (lib/api.ts client, lib/use-live.ts hook, components/live-notice.tsx for loading/offline/empty), rendering the real seeded onchain payments with USDC and enum formatting; disputed rows link to the dynamic verifier; when the backend is offline they show an honest notice rather than fabricated rows. NEXT_PUBLIC_BACKEND_URL sets the base (defaults to localhost:8080). Web auth is now real (Session 34): the simulated demo profile is replaced by backend account sessions. lib/session holds the opaque token store with silent access-token refresh, authFetch, /api/me hydration, and logout; SessionProvider/useSession (mounted in app/layout) expose the account; sign-in is a real Google Identity Services button (components/google-signin) that exchanges the ID token via POST /api/auth/google. The merchant workspace is account-gated (MerchantShell redirects to /signin), while the landing and public verifier stay open. Onboarding seeds name/email from the signed-in account; role/workspace stay local prefs in recourse.demo-profile. To make Google sign-in work, set NEXT_PUBLIC_GOOGLE_CLIENT_ID (web, public) and GOOGLE_CLIENT_ID (backend) to the same Google OAuth Web client id from Google Cloud Console, and add the web origin (e.g. http://localhost:3007) as an authorized JavaScript origin; without it the button shows a clear not-configured state. Sign in with Apple on the web is still deferred: it needs a separate Apple Services ID and a backend audience that accepts it (the current Apple verifier is bound to the iOS bundle com.recourse.buyer); Apple stays the iOS-native path. Remaining web work: the vault deposit tx (money-moving, verify against a funded wallet per R13), exercising Google login end to end once the client id is set, and the dashboard overview still uses illustrative figures.
Sign-in image note: the story panel uses a locally optimized aerial ocean photograph by Alex Perez under the Unsplash License. Provenance is stored in web/public/images/recourse-ocean.LICENSE.txt.
Onboarding layout note: /onboarding uses a Peec-inspired one-decision-per-screen pattern. The active form stays compact on the left, while a contextual Recourse product-proof card sits over a faded dashboard preview on the right. The proof panel is hidden on narrow screens so mobile remains a focused single-column flow. No Peec branding, copy, or testimonial content is reused.
2. backend/ (Rust, actix-web per owner, not axum): the indexer and read API are built and cargo check is clean. backend/ uses actix-web + actix-cors, sqlx (Postgres, runtime queries), and alloy. src/chain.rs reads paymentCount/getPayment/previewVerdict/policyCount/getPolicy/policyHash; src/indexer.rs state-polls Arc every INDEX_INTERVAL_SECS and upserts payments and policies (verdicts from previewVerdict only, R2); src/routes.rs serves /health, /api/payments (optional ?merchant=), /api/payments/{id}, /api/disputes, /api/policies, /api/policies/{id}. Addresses load from deployments/arc-testnet.json at startup (R3). Run: docker compose -f ops/docker-compose.yml up -d, then cargo run in backend/ (see backend/README.md). Run live and verified end to end (Session 22): the indexer read all 8 seeded payments and policy 1 from Arc into Postgres, /health reports indexedPayments 8, and /api/payments, /api/disputes, /api/policies return the correct shapes (payment 5 refundBps 10000 matched, payment 6 refundBps 0, policy window 1209600s, verdict hashes from previewVerdict). Postgres host port is now RECOURSE_PG_PORT (default 5433) because 5432 is commonly taken; on a machine where 5433 is also taken by a native postgres, set RECOURSE_PG_PORT in ops/.env and DATABASE_URL in backend/.env to a free port, and NEXT_PUBLIC_BACKEND_URL in web/.env.local if 8080 is taken too (all three files are gitignored). A code-review pass (Session 21) fixed three P1 correctness bugs: a transient previewVerdict read no longer NULLs a cached verdict (the upsert COALESCEs verdict columns); the projection is scoped to one deployment via an index_meta row so a redeploy truncates rather than shadows stale rows (reset_if_deployment_changed on startup); and /health now probes the DB and returns 503 when Postgres is down instead of a false green. Deferred P2s: this is a state poller, not an event indexer (no tx hashes or receipt events), reads are serial per record, startup SQL has no migration history, and CORS is permissive (tighten when attestor write routes land). The attestor bot is built (Session 23): src/attestor.rs signs the EIP-712 Attestation (domain RecourseAttestor/1/chainId/escrow, struct paymentId,attType,value,deadline) with alloy from ATTESTOR_PK and pushes txs. Two DEMO_MODE-gated endpoints (R6): POST /api/demo/attest {paymentId,value} (signs + submitAttestation, no funds move) and POST /api/demo/resolve {paymentId} (settles, moves funds, logged per R8). It signs objective delivery facts only, never decides outcomes (R4); the onchain PolicyEngine computes the verdict. Signing is byte-verified without the key: a cargo test asserts the local eip712_signing_hash equals the escrow's onchain attestationDigest (golden 0x6132a1...a25d), and a second test confirms sign->recover with legacy v 27/28, so a real attestor signature is accepted onchain. The bot is enabled only when DEMO_MODE and ATTESTOR_PK are set (currently the escrow attestor is the deployer 0xD6c5...983A; hand the bot that key or rotate via setAttestor). The full attest+resolve loop is verified live on Arc: engine/scripts/open-dispute.mjs opened paymentId 9, the bot attested NOT_DELIVERED and resolved, and the verdict flipped from the default DENIED to REFUNDED 100% with chain and indexer verdict hashes matching (0x5d05b6...f2867). The content-addressed evidence blob store is built (POST /api/evidence returns keccak256(blob), GET /api/evidence/{hash} serves it, kept off the disposable projection under EVIDENCE_DIR). The backend was then restructured into layered modules matching the owner's qent conventions: src/app.rs (build_app) plus handlers/*, services/{chain,attestor,evidence} with services/mod.rs holding AppConfig, models/{payment,policy}, and jobs/indexer; behavior is byte-identical, state is injected as separate web::Data per dependency, and sqlx::migrate! replaced the raw-SQL bootstrap. The flat src/{chain,indexer,routes,db,config,attestor,evidence}.rs paths named above are superseded by that layout. Evidence manifest verification is now built (Session 31): services/evidence::compute_evidence_root reproduces the escrow's keccak256(abi.encodePacked(root, evType, hash)) fold from bytes32(0); POST /api/evidence/manifest recomputes it and reads the payment's evidenceRoot straight from the escrow (ChainClient injected into handlers), storing the ordered list only on an exact match and returning 422 otherwise; GET /api/payments/{id}/evidence re-folds and re-checks against the live chain on read; manifests are keyed by deployment (chainId_escrow) since paymentIds restart at 1 on a fresh escrow. The fold is golden-tested against cast keccak vectors. engine/scripts/open-dispute.mjs now uploads evidence, pins it in fileDispute, and posts the manifest; the web verifier (lib/evidence.ts + verify-page) re-folds the backend's list in-browser with viem against the evidenceRoot it read from Arc, so the backend is trusted for the item list only, never the result, and /verify/10 is a settled demo case with a photo and description (evidenceRoot 0x5f2c0b10...22ab; Solidity, Rust, and TypeScript all agree). Write-route authentication is now built (Session 32), per Codex's design call (wallet EIP-712 signatures, one-time challenges, buyer routes split from privileged attestor routes). Two boundaries: buyer writes (evidence upload, manifest) use an EIP-712 wallet signature (no account; the caller proves they are the payment's on-chain buyer), and admin routes (demo attest/resolve) use a bearer token that fails closed and is never shipped to the app. Reads stay public (public chain data; the app can also read Arc directly). Also added: GET /api/payments?buyer=0x.. (plus the existing ?merchant=), and ?limit=/?offset= paging on /payments and /disputes (default 100, max 500). Correctness fixes from the review: nonexistent payments (policyId==0) are rejected on evidence read and in auth, the attestor disables itself on a failed self-check, and cargo fmt is clean.

iOS AUTH CONTRACT (what mobile/ must implement to write): (1) POST /api/auth/challenge -> {nonce (0x 32-byte), expiresAt (unix secs), ttlSecs}. (2) Build and sign this EIP-712 typed data with the buyer wallet key: domain { name: "Recourse", version: "1", chainId: 5042002 } (NO verifyingContract); primaryType "Authorization"; type Authorization(string action, uint256 paymentId, address walletAddress, uint256 chainId, bytes32 bodyHash, bytes32 nonce, uint256 expiresAt). action is "evidence.upload" for a blob or "evidence.manifest" for the manifest; bodyHash = keccak256(exact request body bytes) (for an upload that equals the blob's keccak; for the manifest it is keccak256 of the exact JSON string sent); nonce and expiresAt come from step 1; walletAddress and chainId as signed. (3) Send the write with header X-Recourse-Auth: base64( JSON {action, paymentId, walletAddress, chainId, bodyHash, nonce, expiresAt, signature} ). The nonce is single-use, so fetch a fresh challenge per authenticated request. The backend recovers the signer, requires it to equal getPayment(paymentId).buyer on the live chain, checks the body hash, and consumes the nonce atomically. The digest is golden-tested against viem hashTypedData, so a correct client and the backend agree byte-for-byte; engine/scripts/open-dispute.mjs is a working reference client (see authHeader()). Admin routes just need Authorization: Bearer ADMIN_API_KEY. The buyer never touches ADMIN_API_KEY; normal buyer resolve stays a direct Arc tx from the app. The wrong-wallet branch is covered by engine/scripts/auth-negative-test.mjs (a valid EIP-712 signature from a non-buyer must 403, persist nothing, and not consume its nonce; verified live plus a Postgres check that the nonces stayed consumed=false). The attestor's resolve step is now also a hands-off internal service (Session 33): jobs/resolver settles disputes that are due, mirroring the escrow precondition exactly (status Disputed && (attType != 0 || now >= filedAt + resolveDelay)), re-checking readiness on the live chain per payment, never deciding the verdict, and logging every settlement. It is opt-in via ATTESTOR_AUTO_RESOLVE (off by default so it never surprise-settles) and reuses the attestor wallet; verified live on Arc (paymentId 12 attested, then auto-settled with no manual resolve). Attestation stays an external delivery fact pushed via the admin attest route; the worker only picks the moment settlement is already due. Remaining backend: real account-session auth later (Circle Wallets) can layer on top for profiles/notifications, but wallet signatures stay the authorization proof for payment actions. The web verifier and policy reads stay chain-direct on purpose. Deployment is now code-ready (Session 35), targeting Railway: repo-root Dockerfile + railway.json + .dockerignore, migrations run on boot via sqlx::migrate!, evidence blob store on a /data Railway volume, /health healthcheck, Railway injects PORT; the deploy runbook is in backend/README.md and the owner adds a Postgres service + volume + secrets in Railway. The Dockerfile is provider-agnostic (started on Fly, moved to Railway; only the platform config + runbook changed). CORS is now configurable via CORS_ALLOWED_ORIGINS (permissive only as an empty-value dev fallback). Google sign-in now serves mobile too: GoogleAuthService accepts a set of audiences, so setting GOOGLE_IOS_CLIENT_ID lets the iOS app use the same POST /api/auth/google (aud = iOS client). Email/password auth added: POST /api/auth/email/register and /login (Argon2id hash in accounts.password_hash via migration 0005; 409 on duplicate, uniform 401 on bad login). Passkeys/WebAuthn added (iOS-first, same session): webauthn-rs 0.5 with the danger-allow-state-serialisation feature; migration 0006 (passkey_credentials + single-use webauthn_ceremonies); services/passkey wraps a configured Webauthn (WEBAUTHN_RP_ID/RP_ORIGIN, 503 when unset); four email-first endpoints POST /api/auth/passkey/{register,login}/{start,finish}, register creates the provider='passkey' account on finish and login bumps the signature counter; all reuse the same opaque session. iOS setup: rp_id = the app's apple-app-site-association webcredentials domain, rp_origin = https://<rp_id>, plus the Associated Domains entitlement + a hosted AASA file, so passkeys are exercised against the deployed https domain, not localhost. All verified by cargo check (0), cargo test (14 pass), clippy + fmt clean; the passkey ceremonies are compile/type-verified against the real webauthn-rs API but have no unit tests (need a live authenticator). Not yet run live: the Fly deploy, the mobile Google flow (needs the iOS client id + device), email register/login against Postgres, and the full passkey ceremony (needs the deployed domain + an iOS device). Env note: local disk hit 100% mid-session; freed via rm -rf backend/target, and builds now pass CARGO_PROFILE_DEV_DEBUG=0 to keep target small.

iOS PASSKEY CONTRACT (what mobile/ implements to use WebAuthn via AuthenticationServices). Prereqs: backend has WEBAUTHN_RP_ID (bare domain, e.g. recourse.app) and WEBAUTHN_RP_ORIGIN (https://recourse.app); the app has the Associated Domains entitlement webcredentials:<rp_id>; that domain serves /.well-known/apple-app-site-association = {"webcredentials":{"apps":["<TEAMID>.<bundleid>"]}}. relyingPartyIdentifier passed to ASAuthorizationPlatformPublicKeyCredentialProvider MUST equal WEBAUTHN_RP_ID. Needs a real https domain (no localhost). ALL binary fields are base64url WITHOUT padding (WebAuthn standard): iOS Data -> base64, then + -> -, / -> _, strip =; and reverse (pad back) when decoding challenge/user.id. Every start returns { "challengeId": "<opaque, single-use, 5-min TTL>", "publicKey": <options> }; echo challengeId back on finish and fetch a fresh start per attempt.
(1) REGISTER. POST /api/auth/passkey/register/start { "email", "givenName"?, "familyName"? } -> 200 { challengeId, publicKey: { rp:{id,name}, user:{id(b64url),name,displayName}, challenge(b64url), pubKeyCredParams, authenticatorSelection, excludeCredentials, ... } }; 409 if the email already has a passkey, 400 bad email, 503 off. From publicKey read challenge->Data, user.id->Data (userID), user.name (name), rp.id (== WEBAUTHN_RP_ID). Build ASAuthorizationPlatformPublicKeyCredentialProvider(relyingPartyIdentifier: rpId).createCredentialRegistrationRequest(challenge:name:userID:), run via ASAuthorizationController. On the resulting ASAuthorizationPlatformPublicKeyCredentialRegistration POST /api/auth/passkey/register/finish { challengeId, credential: { id(b64url of credentialID), rawId(same), type:"public-key", response:{ clientDataJSON(b64url of rawClientDataJSON), attestationObject(b64url of rawAttestationObject) } } } -> 200 SessionGrant.
(2) LOGIN. POST /api/auth/passkey/login/start { "email" } -> 200 { challengeId, publicKey: { challenge(b64url), rpId, allowCredentials:[{type,id(b64url),transports}], userVerification, timeout } }; 404 if no passkey for that email. Read challenge + rpId, build provider.createCredentialAssertionRequest(challenge:). On the ASAuthorizationPlatformPublicKeyCredentialAssertion POST /api/auth/passkey/login/finish { challengeId, credential: { id(b64url of credentialID), rawId(same), type:"public-key", response:{ clientDataJSON(b64url of rawClientDataJSON), authenticatorData(b64url of rawAuthenticatorData), signature(b64url of signature), userHandle(b64url of userID) } } } -> 200 SessionGrant; 401 if the assertion fails.
SessionGrant is identical to Apple/Google/email: { accessToken, refreshToken, accessExpiresAt, refreshExpiresAt, account:{ accountId, provider:"passkey", providerUserId, email, givenName, familyName } } -> store the tokens in the Keychain exactly like the other providers. These account tokens authorize profile/onboarding only; wallet EIP-712 signatures still authorize payment writes. Caveat (known, fine for the hackathon): register creates a distinct provider='passkey' account from the caller-supplied, unverified email; production should verify the email or attach the passkey to an already-authenticated Apple/Google account (cheap: passkey_credentials keys by account_id, so it is store-credential-against-existing-account, skipping account creation).
3. engine/: DONE. The policy compiler (compilePolicy, toSpec, PolicyCompileError, name tables in engine/src/compiler.ts) turns authored JSON (PRD section 6) into Rule structs, reusing policyHash and compute (no third impl, R2). Covered by engine/test/compiler.test.ts, whose golden case ties the seed spec to the on-chain policyHash(1). The web policy builder at /policies/new authors rules, live-compiles to the policy hash, shows the compiled structs, and tests a claim through compute. Remaining: publish on-chain (wallet connect, then registerPolicy from the merchant address).
4. USYC access: apply; when approved, write USYCTellerAdapter and redeploy.

Demo state is seeded and verified on Arc (deployments/seed-arc-testnet.json): policyId 1,
8 payments, payment 5 REFUNDED 100%, payment 6 DENIED, payment 7 vault-advanced.
Re-seed by rerunning `node engine/scripts/seed.mjs` (produces a new policy + payments).

Done: deterministic core (M0), TS engine mirror with hash parity (M2), the stateful contract layer with integration tests (M1), the deploy + codegen pipeline, a live on-chain deployment to Arc testnet, the public verify page, dashboard overview, full marketing landing page, and role-aware demo onboarding. The landing page now covers the product value, four-step protection flow, live verdict verification, buyer and merchant and LP value, vault return mechanics, FAQs, and a final demo CTA while keeping every claim tied to the implemented prototype. If the engine or vectors change, regenerate hashes.json (forge script script/GenVectorHashes.s.sol:GenVectorHashes) and keep forge and vitest green in one commit (R2).

## Standing rules

From the docs (each exists because breaking it sinks the project):

- R1. USDC flows through the ERC-20 interface at 6 decimals only. Never read native (18 decimal) balance for product logic.
- R2. The verdict engine exists exactly twice: canonical Solidity and the TS mirror, chained by packages/vectors. Any engine change updates the vectors and passes both forge and vitest in the same commit. No third implementation; Rust and Swift call previewVerdict or read the API.
- R3. Contract addresses come only from deployments/arc-testnet.json via codegen. A hardcoded address anywhere is a bug.
- R4. The Rust backend holds no business logic: index, store evidence, serve reads, sign demo attestations.
- R5. The native iPhone app serves the buyer only. Merchant and LP live on the web. Android is deferred and may use Flutter later.
- R6. Demo-only endpoints are gated behind DEMO_MODE and labeled as such in code.
- R7. Testnet only. No mainnet RPC in any script, config, or env. The testnet keys are throwaways used nowhere else.
- R8. Never run the seeder or attestor bot against a deployment mid-rehearsal without logging it.
- R9. No em dashes anywhere: UI copy, docs, comments, commit messages. Use commas, colons, or parentheses.

From the owner (this repo):

- R10. Comment with purpose only. A comment explains why (intent, a constraint, a gotcha, a spec reference), never restates what the code plainly does.
- R11. Do not push to GitHub. Commit locally only unless explicitly told to push. No Co-Authored-By: Claude trailer and no "Generated with Claude Code" line in commit messages.
- R12. Confirm the approach before writing non-trivial code: research the tradeoffs, check the pattern against the docs, and split work across subagents where it helps.
- R13. Verify money-moving scripts against a real node (anvil), not just forge simulation. Simulation runs every tx at one block timestamp and hides time-dependent behavior (yield-index drift, rounding); only a live broadcast with advancing timestamps exposes it.

## Session close checklist

Append to log.md (newest first: found, fixed or decided, rule earned), update this file, commit locally with a clean message.
